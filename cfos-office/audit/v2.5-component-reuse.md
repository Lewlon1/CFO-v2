# v2.5 Component Reuse Audit

**Run date:** 2026-05-19
**Branch:** `claude/v2.5-ia-simplification-AWnKN`
**Author:** Claude (session v2.5)
**Purpose:** Establish ground truth on shared-primitive adoption before touching tokens, schema, routes, and dashboards in Phases 1–4. Drives Phase 5 fix list.

---

## Q1 — Primitive usage matrix

The shared primitives are at `src/components/office/dashboards/` (not `shared/` as the session plan assumed — the import alias remains `'./Briefing'` etc. since all consumers are siblings).

| Primitive | Consumers | Inline-pattern dupes |
|---|---|---|
| `Briefing` | `CashFlowDashboard:8`, `NetWorthDashboard:6`, `ValuesDashboard:6`, `ScenariosDashboard:5` | None outside the primitive — `"— C."` signature comes from system prompts, not UI |
| `DetailHeader` | `CashFlowDashboard:10`, `NetWorthDashboard:8`, `ValuesDashboard:7`, `ScenariosDashboard:6` | None — the only other `ChevronLeft` usage is unrelated (`InlineRow.tsx:75` uses `ChevronRight`) |
| `DrillDownRow` | `CashFlowDashboard:11`, `NetWorthDashboard:9`, `ValuesDashboard:8`, `ScenariosDashboard:7` | `InlineRow.tsx:75` uses `ChevronRight` in a different context (chat result rows) — not a refactor candidate |
| `DashboardEmptyState` | `CashFlowDashboard:9`, `NetWorthDashboard:7` | `ScenariosDashboard:128` ("No scenarios running yet" rendered inline); `ValuesDashboard` does not render an empty state at all |
| `FolderSection` | `OfficeHomeClient:5` only | n/a |
| `FolderCard` (from `src/components/data/FolderCard.tsx`) | **Zero consumers** (only re-exported by `src/components/data/index.ts:3`) | Dead code — defer-to-backlog candidate |

**Verdict:** The four primitive types (`Briefing`, `DetailHeader`, `DrillDownRow`, `DashboardEmptyState`) are well-adopted across the four office dashboards. No inline duplicates of the patterns exist outside the dashboards directory. The work to extract these in earlier sessions is holding.

---

## Q2 — Inline helper duplications

### `formatCurrency` — seven sources, no single canonical home

| File | Type | Line |
|---|---|---|
| `src/lib/constants/dashboard.ts:55` | **Shared export** | `formatCurrency(amount, currency)` |
| `src/lib/utils/format-currency-rounded.ts:11` | **Shared export** | `formatCurrencyRounded(amount, currency)` — used by all four dashboards |
| `src/lib/analytics/pattern-detectors.ts:17` | Module-local export | `formatCurrency` |
| `src/components/trips/TripsClient.tsx:7` | Inline duplicate | `function formatCurrency(...)` |
| `src/components/chat/savedCardBuilders.ts:6` | Inline duplicate | `function formatCurrency(...)` |
| `src/components/chat/TripPlanResult.tsx:3` | Inline duplicate | `function formatCurrency(...)` |
| `src/components/chat/ScenarioResult.tsx:13` | Inline duplicate | `function formatCurrency(...)` |
| `src/app/(office)/office/cash-flow/patterns/PatternsClient.tsx:13` | Inline duplicate | `function formatCurrency(...)` |

**Verdict:** Five inline duplicates plus two shared exports plus one variant (`Rounded`). No `src/lib/format/currency.ts` exists. Phase 5 candidate: extract one canonical helper, migrate duplicates.

### `formatMonth` / `formatMonthShort`

| File | Line | Notes |
|---|---|---|
| `src/lib/constants/dashboard.ts:60` | Shared `formatMonth` |
| `src/lib/utils/format-currency-rounded.ts:26` | Shared `formatMonthShort` (used by dashboards) |
| `src/components/dashboard/ReviewBanner.tsx:13` | Inline `formatMonthName` |
| `src/lib/ai/review-context.ts:57` | Inline `formatMonth` (server-side, separate intent) |

### `buildBriefing` per-dashboard

Each dashboard has its own `buildBriefing` helper — semantically necessary (each dashboard formats different inputs into briefing prose), so this is **not** duplication to fix.

- `ValuesDashboard:117`
- `CashFlowDashboard:132`
- `NetWorthDashboard:127`
- `ScenariosDashboard:113` (deleted in Phase 3)

### Empty-state inline pattern

`ScenariosDashboard:128` renders "No scenarios running yet" inline rather than using `DashboardEmptyState`. Since the whole dashboard is being deleted in Phase 3, this inline divergence dies with it — no fix needed.

---

## Q3 — Token vs hex usage

`folderColors` token canonical source: `src/lib/tokens.ts`. Hardcoded hexes are found in 30+ locations. Listing the high-value cases only — exhaustive list lives in the Phase 0 raw findings.

### Folder-colour hexes (token violations)

| Hex | Meaning | Hardcoded at |
|---|---|---|
| `#E8A84C` | Values amber (deprecated in Phase 1) / Gold (CFO) | `src/lib/tokens.ts:22` (gold), `globals.css:15,22,41`, `ValuesDashboard.tsx:13`, `CashFlowDashboard.tsx:16` (mis-named `ACCENT` const, though Cash Flow's ACCENT is actually `#22C55E` — confirmed below), plus ~25 component-level uses in value-map, chat results, onboarding, dashboard charts |
| `#22C55E` | Positive / Cash Flow | `tokens.ts:28`, `globals.css:47`, `CashFlowDashboard.tsx:16` (the `ACCENT` literal duplicates the token), plus `DataComponents.tsx`, `OfficeMonthlyOverview.tsx`, `CashFlowSection.tsx`, onboarding |
| `#06B6D4` | Net Worth cyan | `tokens.ts:31`, `globals.css:50`, `NetWorthDashboard.tsx:14` (`ACCENT` const), plus `NetWorthSection.tsx`, `balance-sheet/route.ts`, onboarding, charts |
| `#F43F5E` | Scenarios red (folder gone, retained as `--negative`) | `tokens.ts:29`, `globals.css:48`, `ScenariosDashboard.tsx:11` (deleted Phase 3), plus negative-delta indicators across the app |
| `#9C7B2C` | Goals brass | `tokens.ts:49` (token, no leakage) |
| `#8B5CF6` | Burden purple (deprecated Phase 1) | `tokens.ts:32`, `globals.css:51`, `dashboard.ts:9`, plus `balance-sheet/route.ts`, `TripPlanResult.tsx:14`, onboarding |
| `#3B82F6` | Info blue / Investment value | `tokens.ts:30`, `globals.css:49`, `dashboard.ts:3`, plus charts |

### Tailwind class hexes (Burden disagreement)

- Amber class chains in 13+ files (bills, profile, trips, value-map, archetype reveal). Most are not Burden-semantic; they're warning/highlight uses. Not all need migration.
- Purple class chains: `GoalCard.tsx:105,154,169` — these are NOT Burden semantic; they're Goal-status highlight. Likely fine to leave; **add a token** for "highlight purple" if needed in Phase 5.

### Cross-reference: the four-way Burden disagreement (Q5 deep-dive)

| Source | Burden value | Comment |
|---|---|---|
| `src/lib/tokens.ts:60` | `#8B5CF6` | Authoritative |
| `src/app/globals.css:51` | `--accent-purple: #8B5CF6` | Aligned with tokens |
| `src/lib/constants/dashboard.ts:39-45` | `bg-amber-500/10 text-amber-400` | **Disagrees** — uses Tailwind classes, not hex; semantically amber, not purple |
| `src/lib/value-map/constants.ts:27` | `#E8A84C` (gold/amber) | **Disagrees with tokens** — same hex as Values gold |

The dashboard.ts Tailwind chain and value-map hex both predate the `tokens.ts` definition. Three of four files disagree with the source of truth. Phase 1 fixes `tokens.ts` (`#9C8B7A` warm grey) and `globals.css`; Phase 5 has the dashboard.ts and Tailwind class migration.

### Foundation / Investment / Leak cross-reference

Same four files, additional disagreements:

| Category | tokens.ts | globals.css | dashboard.ts | value-map/constants.ts |
|---|---|---|---|---|
| Foundation | `#22C55E` | `--positive: #22C55E` | `bg-blue-500/10 text-blue-400` | `#4A90D9` |
| Investment | `#3B82F6` | `--info: #3B82F6` | `bg-emerald-500/10 text-emerald-400` | `#48BB78` |
| Leak | `#F43F5E` | `--negative: #F43F5E` | `bg-red-500/10 text-red-400` | `#E53E3E` |

Foundation and Investment are **inverted** between `tokens.ts` and `value-map/constants.ts` — what tokens calls "foundation green / investment blue", value-map calls "foundation blue / investment green". This is a real semantic bug that needs unwinding, but it's a value-map-flow UX question (does the quadrant matrix render Foundation as blue or green?), not a v2.5 in-scope fix. **BACKLOG.**

---

## Q4 — Dashboard implementation parity

| Dashboard | Briefing | DetailHeader | DrillDownRow | Empty state | `formatCurrency` source | `buildBriefing` |
|---|---|---|---|---|---|---|
| `CashFlowDashboard` | ✓ L61 | ✓ L44 | ✓ L74–110 | ✓ `DashboardEmptyState` L52 | imports `formatCurrencyRounded` L13 | inline L132 |
| `ValuesDashboard` | ✓ L58 | ✓ L56 | ✓ L75–109 | none (no empty state) | imports `formatCurrencyRounded` L10 | inline L117 |
| `NetWorthDashboard` | ✓ L55 | ✓ L39 | ✓ L64–95 | ✓ `DashboardEmptyState` L47 | imports `formatCurrencyRounded` L11 | inline L127 |
| `ScenariosDashboard` | ✓ L74 | ✓ L72 | ✓ L85–105 | **inline** L128 | imports `formatCurrencyRounded` L9 | inline L113 |

**Verdict:** Three of four dashboards are aligned. The two divergences (`ValuesDashboard` has no empty state; `ScenariosDashboard` inlines its empty state) are both about Scenarios going away anyway — Values having no empty state is an honest design choice (Values is computed from transaction history; empty case is unreachable for any user with data).

---

## Q5 — What-If tool preservation check

Phase 3 deletes `/office/scenarios/what-if/page.tsx`. The session plan requires verifying the chat tool still exists.

**Found:** `src/lib/ai/tools/model-scenario.ts` exists. Registered in `src/lib/ai/tools/index.ts:7` and exposed in the toolbox as `model_scenario`. Chat can still model what-if scenarios after Phase 3. **Safe to proceed with deletion.**

---

## Findings classified

### MUST FIX in v2.5 (in scope — files we're touching anyway)

1. **Burden colour Tailwind chain in `dashboard.ts`** — Phase 1 plan already covers (`bg-amber-* → bg-stone-*`). Confirmed by audit.
2. **Burden hex in `value-map/constants.ts`** — Phase 1 plan already covers (`#E8A84C → #9C8B7A`). Confirmed.
3. **`ScenariosDashboard.tsx` deletion** — Phase 3 plan already covers. Confirmed by audit no other consumer references its types except `src/app/(office)/office/scenarios/page.tsx`.
4. **`ScenariosSection.tsx`** — Phase 4 plan removes the home section. Confirmed `OfficeHomeClient:137` is the only consumer; safe to delete the section component too (no other importers).
5. **Folder hex token violations introduced/touched in Phase 1** — palette migration needs to leave the touched files token-clean. Specifically `ValuesDashboard.tsx:13` and `NetWorthDashboard.tsx:14` (`ACCENT` literals). Migrate these to `folderColors.values` and `folderColors.networth` references.
6. **`CashFlowDashboard.tsx:16` `ACCENT` literal** — same as above. Migrate to `folderColors.cashflow`.

### SHOULD FIX in v2.5 (small, in-neighborhood)

7. **Extract `formatCurrency` to `src/lib/format/currency.ts`** — the canonical home referenced by the plan. Migrate the five inline duplicates to the new module. Keeps Phase 5 bounded: the dashboards already import `formatCurrencyRounded` (different beast), so they don't need touching. The targets are `TripsClient.tsx`, `savedCardBuilders.ts`, `TripPlanResult.tsx`, `ScenarioResult.tsx`, `PatternsClient.tsx`. **Caveat:** the existing `dashboard.ts:55` `formatCurrency` is currency-aware (takes `currency` arg); the inline duplicates may differ subtly. Migration must check signatures before swapping.

### BACKLOG (defer; entries to be added to `BACKLOG.md` in Phase 5)

8. **Foundation/Investment colour inversion** between `tokens.ts` (foundation green, investment blue) and `value-map/constants.ts` (foundation blue, investment green). This is a UX-bearing decision — which colour means which category in the Value Map quadrant? — not a refactor. **BACKLOG entry:** "Reconcile foundation/investment colour assignments between tokens.ts and value-map/constants.ts. Tokens says foundation=green, investment=blue; value-map says the opposite. Pick the source of truth, migrate the other."
9. **`FolderCard` is dead code** — exported from `src/components/data/index.ts` but has zero consumers. **BACKLOG entry:** "Delete `src/components/data/FolderCard.tsx` + remove from `src/components/data/index.ts`. Confirmed zero consumers in v2.5 audit."
10. **`formatCurrencyRounded` vs `formatCurrency`** — two utilities with similar names doing different things (one rounds to whole units, the other shows decimals). Reasonable as-is but worth consolidating into a single function with a `rounded?: boolean` option. **BACKLOG.**
11. **GoalCard purple Tailwind classes** at `GoalCard.tsx:105,154,169` — not Burden semantic, but worth a "highlight purple" token to avoid future drift. **BACKLOG.**
12. **`OfficeMonthlyOverview.tsx`, `DataComponents.tsx`, `balance-sheet/route.ts` hardcoded folder hexes** — outside the file surface that Phases 1–4 touch. **BACKLOG entry:** "Migrate hardcoded `#22C55E`, `#06B6D4`, `#3B82F6`, `#F43F5E` references in `OfficeMonthlyOverview.tsx`, `DataComponents.tsx`, `balance-sheet/route.ts` (etc.) to `folderColors.*` / value-category tokens. Audit catalogued ~25 sites."
13. **`formatMonth` triple-implementation** (`dashboard.ts:60`, `format-currency-rounded.ts:26`, `ReviewBanner.tsx:13`, `review-context.ts:57`). **BACKLOG.**

---

## Halt check

Total findings across all categories: 13. None of the four categories produced >20 items. Session scope confirmed; proceed to Phase 1.

---

## Per-finding status (updated as Phase 5 runs)

1. **MUST FIX 1** — Burden colour Tailwind chain in `dashboard.ts` → ✓ fixed in commit 8736772 (Phase 1)
2. **MUST FIX 2** — Burden hex in `value-map/constants.ts` → ✓ fixed in commit 8736772 (Phase 1)
3. **MUST FIX 3** — `ScenariosDashboard.tsx` deletion → ✓ deleted in commit 5e2abbc (Phase 3)
4. **MUST FIX 4** — `ScenariosSection.tsx` deletion → ✓ deleted in commit 5e2abbc (pulled forward from Phase 4 — `OfficeHomeClient` Scenarios card gone in Phase 3)
5. **MUST FIX 5** — `ValuesDashboard.tsx:13` + `NetWorthDashboard.tsx:14` ACCENT literals → ✓ migrated to `folderColors.values` / `folderColors.networth` in Phase 5
6. **MUST FIX 6** — `CashFlowDashboard.tsx:16` ACCENT literal → ✓ migrated to `folderColors.cashflow` in Phase 5
7. **SHOULD FIX 7** — extract `formatCurrency` to `lib/format/currency.ts` → ✓ extracted with 13 unit tests in Phase 5. Migrated `TripsClient`, `TripPlanResult`, `ScenarioResult`, `savedCardBuilders` (4 of 5 targets). `PatternsClient` deferred — its `maxFractionDigits-only` shape isn't supported by the new helper API; BACKLOG entry added.
8. **BACKLOG 8** — Foundation/Investment colour inversion → BACKLOG.md entry added
9. **BACKLOG 9** — `FolderCard` dead code → BACKLOG.md entry added
10. **BACKLOG 10** — `formatCurrencyRounded` vs `formatCurrency` consolidation → BACKLOG.md entry added
11. **BACKLOG 11** — `GoalCard` purple Tailwind tokenisation → BACKLOG.md entry added
12. **BACKLOG 12** — hardcoded folder hexes in non-dashboard files → BACKLOG.md entry added
13. **BACKLOG 13** — `formatMonth` triple-implementation → BACKLOG.md entry added
