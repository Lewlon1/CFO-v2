# Pre-Session 32C Audit

Date: 2026-05-26
Branch: `session-32/the-read`
Worktree: `/Users/lewislonsdale/Documents/CFO-V2/.claude/worktrees/competent-shirley-edd7d6`

## 0.1 Session A & B deliverables

| File | Status |
|---|---|
| `cfos-office/docs/the-layers.md` | ✓ present |
| `cfos-office/src/lib/feature-flags/layered-read.ts` | ✓ present |
| `cfos-office/src/lib/analytics/cluster-behaviour/index.ts` | ✓ present |
| `cfos-office/src/lib/analytics/chat-signals/extract.ts` | ✓ present |
| `cfos-office/src/lib/ai/tools/get-cluster-behaviour.ts` | ✓ present |
| `cfos-office/src/lib/ai/tools/get-conversation-signals.ts` | ✓ present |
| `cfos-office/src/app/api/insights/post-upload/route.ts` | ✓ present (with `handleLayeredFirstRead`) |
| `cfos-office/src/components/onboarding/Welcome.tsx` | ✗ does not exist by this name (path moot — onboarding-v2 is route-based) |
| `cfos-office/src/components/onboarding/Processing.tsx` | ✗ does not exist by this name (path moot — onboarding-v2 is route-based) |

Session B is route-based; the plan-named `Welcome.tsx`/`Processing.tsx` are not load-bearing for Session C work. The layered first Read path is fully integrated and persists composition metadata on `conversations.metadata.first_read_metadata`.

## 0.2 Chat infrastructure

| Item | Finding |
|---|---|
| Assistant message component | `MessageList.tsx` renders all messages inline. There is **no** `AssistantMessage.tsx`. Plan must adapt: wire ResonanceTap + scroll observer + event tracking directly into the assistant branch of `MessageList`. |
| Chip tap handler | `TappableOptions` with `onSelect` callback flows up through `MessageList` → `ChatProvider.handleOptionSelect` → `sendMessage`. Hook tracking on the `onSelect` callback. |
| Message-type field | **There is no `message_type` column.** First-insight messages are identified by joining `messages → conversations` where `conversations.type = 'first_insight'` AND `metadata->>'layered_read' = 'true'`, then taking the first assistant message ordered by `created_at`. |
| Message DB ID exposure | `chat/route.ts` already attaches `messageDbId` to assistant message metadata. `MessageFeedback` consumes this via `message.metadata.messageDbId` — same pattern works for ResonanceTap. |
| Existing scroll tracking | None. Each message render must own its IntersectionObserver. |
| Existing feedback pattern | `MessageFeedback.tsx` POSTs to `/api/analytics/feedback` (fire-and-forget). Mirror this fire-and-forget pattern for `/api/wow/event`. |
| Existing first_insight metadata | `conversations.metadata.first_read_metadata` = `{ layers_used, features_cited, gap_present, clusters_referenced }` (from `extractCompositionMetadata` in `compose-first-read.ts`). |

### Plan field-name corrections

| Plan name | Actual name |
|---|---|
| `chat_messages` table | `messages` table |
| `chat_conversations` table | `conversations` table |
| `chat_messages.message_type = 'first_insight'` | `conversations.type = 'first_insight'` (no message-level column) |
| `chat_messages.metadata.predicted_wow_score` etc. | Not present today — `wow_assessments` will record `NULL` initially. Lewis can backfill via `scripts/compare-first-insight.ts` once the layered judge is wired. |

### messages table schema (confirmed)
```
id, conversation_id, user_id, role, content,
actions_created (Json), profile_updates (Json), insights_generated (Json),
tools_used (string[]), prompt_tokens, completion_tokens,
created_at, deleted_at, anonymised_at
```

### conversations table schema (confirmed)
```
id, user_id, type, status, metadata (Json), title,
analysed_at, profile_extracted_at, created_at, updated_at, deleted_at, anonymised_at
```

## 0.3 Persona infrastructure

- Personas live at `cfos-office/tests/onboarding/personas/<id>.ts` (flat, not nested under `first-insight/`)
- Each persona is a single TS file exporting a `Persona` object: profile + valueMapResponses + CSV (inline base64) + expectations
- Eval mechanism: `npm run test:onboarding` runs the full Playwright suite. Per-persona judge call happens in `judge-first-insight.ts`. No standalone `eval:personas` script.
- Separate judge calibration system at `eval/` + `scripts/eval/` (calibrate, diagnose, tournament, promote). This is Lewis's weekly champion-judge loop, not the persona eval.

### Existing personas

| ID | Coverage | Plan equivalent |
|---|---|---|
| `builder-classic` | Builder, investment-focused | — |
| `fortress-saver` | Thrifty, foundation-heavy, disciplined | **Covers Henrik** |
| `truth-teller-balanced` | Balanced across quadrants | — |
| `drifter-expat` | Lewis-style expat, EUR, high dining/subs | **Partial coverage of Zane** (EUR but not ES-specific) |
| `anchor-debt` | Debt-heavy burden profile | **Covers Imani** |
| `time-saver-expert` | Builder + advice-averse high-income | — |
| `skip-value-map` | Chat-first wealth struggle | — |
| `skip-csv-upload` | Chat-first debt struggle | — |

### Genuine persona gaps (proposed for Phase 6)

| Plan persona | Status | Reasoning |
|---|---|---|
| Imani (debt-heavy) | **Skip** — covered by `anchor-debt` | Riley/Leeds with credit-card / personal-loan / car-finance repayments. Identical archetype. |
| Henrik (disciplined) | **Skip** — covered by `fortress-saver` | Already represents foundation-heavy disciplined profile. |
| Aiko (low-transaction, 21d data) | **Add** | No existing persona has a deliberately sparse window — tests that the CFO doesn't over-claim from thin data. Highest-value gap. |
| Sofia (chaotic / freelancer) | **Add** | No existing freelance / irregular-income persona. Tests behavioural-feature confidence under high variance. |
| Tom (long history, 18 months) | **Add** | No existing persona exceeds 90 days. Tests the 90-day default window correctness. |
| Zane (Spain / EUR / Spanish merchants) | **Add** | `drifter-expat` is EUR but expat-flavoured; need a true ES persona with Spanish-language merchant names + Spain-specific bills. |

This gives **4 new personas** instead of 6 (Imani and Henrik would duplicate existing coverage).

## 0.4 Constraints inherited from existing code

- `vercel.json` already configured with `dub1` region and 7 existing crons. Adding `wow-aggregate` to that array.
- `CRON_SECRET` already used by other crons (e.g. `portrait-extraction`) — established pattern. Bearer-token check is consistent across handlers.
- Service-role client at `@/lib/supabase/service` — already used by `compose-first-read.ts` and `post-upload/route.ts`. Reuse for the cron.
- Server-side authenticated client at `@/lib/supabase/server` — already used across `/api/*` routes. Reuse for the `/api/wow/event` endpoint.
- Test infrastructure: existing `__tests__/` subdirs sit alongside source. Place `realised-score.test.ts` at `src/lib/wow/__tests__/realised-score.test.ts` (not at top-level `tests/wow/`) to match convention.

## 0.5 Open questions resolved without Lewis

1. **Use `messages` table.** Plan's `chat_messages` is wrong for this codebase.
2. **No `message_type` column.** Identify first_insight messages via conversation type + first assistant message.
3. **predicted_wow_score and judge_id are NULL on creation.** No judge wired into compose-first-read yet; Lewis can backfill via the eval/ champion loop.
4. **Persona expansion scoped to 4 net-new** (Aiko, Sofia, Tom, Zane) — Imani and Henrik are redundant with existing personas.
5. **Tests live alongside source** at `__tests__/`.
6. **No standalone `eval:personas` script.** Phase 6 uses `npm run test:onboarding` (full Playwright run) for end-to-end persona validation. Lewis approves cost (~$1-2 per full run via Bedrock judge).

## 0.6 Plan acceptance

Plan acceptable with these adaptations. Beginning Phase 1 (database schema).
