// Stub — AI categorisation will be implemented in Session 3
export async function aiCategoriseBatch(
  _merchants: unknown[],
  _categories?: string[]
): Promise<Map<string, { categoryName: string }>> {
  return new Map()
}
